import { Entity } from 'src/architecture/contracts/entity/entityContract';
import { Level } from 'src/architecture/contracts/level/levelContract';

export class Renderer {
    renderEntities(entities: Entity[]): void {
        // Implementation to render entities
    }

    renderLevel(level: Level): void {
        // Implementation to render level
    }
}
